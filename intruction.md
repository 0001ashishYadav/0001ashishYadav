Git Download & install

open Github
create a repository (if required)

copy remote url of your repository

---

open VS code
initialise a empty repository using

```node
git init
----Changing branch to main---
git branch -M main
---- Adding Remote URL ---
git remote add your-origin remote-url
```
